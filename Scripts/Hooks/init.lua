return {
  Utils = require("Utils"),
  Aircraft = require("Aircraft"),
  Player = require("Player"),
  DCSTimer = require("DCSTimer"),
  EventSource = require("EventSource"),
  BaseContactSource = require("BaseContactSource"),
}
